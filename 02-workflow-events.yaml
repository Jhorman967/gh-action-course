name: 02 - Workflow Events

on: push

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "HEvento que desencadeno: ${{ github.event_name }}"