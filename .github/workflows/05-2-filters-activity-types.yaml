name: 05 - 2 - Event Filters and Activity Types

on: 
    push:
        branches: [ main ]
    pull_request:
            types: [ closed ]

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
        - name: echo text
          run: Running whenever a PR is closed ${{github.event_name}}